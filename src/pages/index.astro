---
import Layout from '@/layout/layout.astro';
import HeroHomepage from '@/components/heroHomepage';
import AboutMeHomepage from '@/components/aboutMeHomepage';
import DocumentosHomepage from '@/components/documentsHomepage';
import TechStackHomepage from '@/components/techStackHomepage';
import ClientsHomepage from '@/components/clientsHomepage';
import MarqueeHomepage from '@/components/marqueeHomepage';
import ContactMeHomepage from '@/components/contactMeHomepage';
import FooterHomepage from '@/components/footerHomepage';

const visible = { rootMargin: '400px' };
---

<Layout>
  <main class='flex flex-col'>
    <div class='min-h-svh flex'>
      <HeroHomepage client:load />
    </div>
    <AboutMeHomepage client:visible={visible} />
    <DocumentosHomepage client:visible={visible} />
    <TechStackHomepage client:visible={visible} />
    <ClientsHomepage client:visible={visible} />
    <MarqueeHomepage client:visible={visible} />
    <ContactMeHomepage client:visible={visible} />
  </main>
  <FooterHomepage client:visible={visible} />
</Layout>
