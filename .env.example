# The deployed origin, with no trailing slash. Everything SEO is built from it:
# canonical, hreflang, sitemap.xml, robots.txt and the og:image url. Leave it
# unset and the build warns and falls back to http://localhost:4321.
PUBLIC_SITE_URL=https://example.com
